# ── Dependencies ──
node_modules/
server/node_modules/

# ── Build output ──
dist/
dist-ssr/

# ── SQLite database (regenerated by seed on deploy) ──
server/clinic.db

# ── Environment / secrets ──
.env
.env.local
.env.*.local

# ── Vite cache ──
.vite/

# ── Logs ──
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── OS artifacts ──
.DS_Store
Thumbs.db

# ── Editor directories ──
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Misc ──
*.local

# NOTE: public/clinic_erp.db and public/erd.png ARE committed (legacy browser DB + ERD image)
